% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-rcpp.R
\name{predict.magp}
\alias{predict.magp}
\alias{predict.magp2d}
\alias{predict.magpfull}
\title{Predict outcomes from a fitted MaGP model}
\usage{
\method{predict}{magp2d}(
  object,
  newdata,
  se.fit = FALSE,
  type = c("script", "response", "latent"),
  ...
)

\method{predict}{magpfull}(
  object,
  newdata,
  se.fit = FALSE,
  type = c("script", "response", "latent"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{magp2d} or \code{magpfull} object.}

\item{newdata}{A numeric matrix or data frame with the same quantitative and
sequence inputs used for fitting. A response column named \code{y} is ignored.}

\item{se.fit}{Logical; if \code{TRUE}, return predictive standard errors and
variances in addition to fitted values.}

\item{type}{Prediction convention. \code{"script"} uses the fitted training-row
convention when \code{newdata} exactly matches a training row. \code{"response"}
includes the nugget variance for a future response, and \code{"latent"}
returns uncertainty for the noise-free surface.}

\item{...}{Additional arguments, currently unused.}
}
\value{
If \code{se.fit = FALSE}, a numeric vector of predictions. Otherwise, a
list with components \code{fit}, \code{se.fit}, \code{variance}, and \code{type}.
}
\description{
Returns predictions for new quantitative-sequence inputs. Plug-in standard
errors and variances can be returned with the predictions.
}
\examples{
\donttest{
train <- read.table(
  system.file("extdata", "example_train.txt", package = "magp"),
  header = TRUE
)
test <- read.table(
  system.file("extdata", "example_test.txt", package = "magp"),
  header = TRUE
)
fit <- magp2d_fit(train, seed = 1)
predict(fit, test[1:3, ], se.fit = TRUE, type = "response")
}
}
