% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initial-design-complete.R
\name{magp_initial_design}
\alias{magp_initial_design}
\title{Construct a quantitative-sequence initial design}
\usage{
magp_initial_design(
  n,
  q,
  pair_weight = 0.2,
  sequence_space_weight = 0.8,
  quantity_weight = 0.5,
  sequence_weight = 0.5,
  p = 15L,
  sequence_maxit = 10000L,
  quantity_maxit = 10000L,
  alignment_maxit = 10000L,
  sequence_temp = 0.1,
  quantity_temp = 0.01,
  alignment_temp = 0.001,
  tmax = 10L,
  initial_sequence = NULL,
  initial_quantity = NULL,
  seed = NULL,
  sequence_method = c("sann", "random", "sfta"),
  sfta_control = list()
)
}
\arguments{
\item{n}{Number of design runs. Must be at least two.}

\item{q}{Number of components. Must be at least three.}

\item{pair_weight}{Nonnegative weight for ordered adjacent-pair balance in
the sequence search.}

\item{sequence_space_weight}{Nonnegative weight for Hamming-distance space
filling in the sequence search.}

\item{quantity_weight}{Nonnegative quantitative-distance weight in the
joint criterion.}

\item{sequence_weight}{Nonnegative sequence-distance weight in the joint
criterion.}

\item{p}{Positive whole-number exponent used in all three criteria.}

\item{sequence_maxit}{Positive whole number of sequence-search iterations.}

\item{quantity_maxit}{Positive whole number of quantitative-search
iterations.}

\item{alignment_maxit}{Positive whole number of row-alignment iterations.}

\item{sequence_temp}{Positive initial temperature for the sequence search.}

\item{quantity_temp}{Positive initial temperature for the quantitative
search.}

\item{alignment_temp}{Positive initial temperature for the alignment search.}

\item{tmax}{Positive whole number of evaluations at each temperature.}

\item{initial_sequence}{Optional \code{n} by \code{q} matrix of sequence positions.}

\item{initial_quantity}{Optional \code{n} by \code{q} Latin hypercube in \verb{[0, 1]}.}

\item{seed}{Optional nonnegative whole-number seed. Separate deterministic
seeds are used for the three stages, and the caller's random-number state
is preserved.}

\item{sequence_method}{Method for the sequence portion. Choose \code{"random"},
\code{"sfta"} for space-filling threshold accepting, or \code{"sann"} for simulated
annealing. The default is \code{"sann"} for backward compatibility.}

\item{sfta_control}{Named list of SFTA settings, passed to
\code{\link[=magp_sequence_design]{magp_sequence_design()}}. Used only for \code{sequence_method = "sfta"}.}
}
\value{
An object of class \code{magp_initial_design}. Its \code{design} element is an
\code{n} by \code{2*q} matrix ready to use as the input to a MAGP fitting function.
The first \code{q} columns contain quantitative levels and the last \code{q} columns
contain sequence positions. The component searches and their criterion
values are retained for inspection.
}
\description{
Builds an initial design in three steps. It first generates the sequence
permutations, then constructs a maximin-style Latin hypercube for the
quantitative levels, and finally aligns the two fixed designs by permuting
whole quantitative rows. The final alignment preserves both the Latin
hypercube and every sequence permutation.
}
\details{
The sequence method does not change how quantitative levels are
generated. With a fixed \code{seed}, all three methods use the same quantitative
design before the final row-alignment step. The alignment can change its
row order but not its values or Latin-hypercube structure.
}
\examples{
design <- magp_initial_design(
  n = 8,
  q = 4,
  sequence_maxit = 500,
  quantity_maxit = 500,
  alignment_maxit = 500,
  seed = 1
)
design$design
design$criteria

random <- magp_initial_design(
  8, 4, sequence_method = "random", seed = 1,
  quantity_maxit = 100, alignment_maxit = 100
)
sfta <- magp_initial_design(
  8, 4, sequence_method = "sfta", seed = 1, sequence_maxit = 200,
  quantity_maxit = 100, alignment_maxit = 100,
  sfta_control = list(nstarts = 2, ncalibrate = 50)
)
rbind(random = random$criteria, sfta = sfta$criteria)

}
\references{
Xiao, Q., Wang, Y., Mandal, A., and Deng, X. (2024).
Modeling and Active Learning for Experiments with Quantitative-Sequence
Factors. Journal of the American Statistical Association.
\doi{10.1080/01621459.2022.2123335}.
}
