% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-rcpp.R
\name{magpfull_fit}
\alias{magpfull_fit}
\title{Fit a MaGP model with a full sequence map}
\usage{
magpfull_fit(
  X,
  y = NULL,
  q = NULL,
  tau = 0.001,
  maxeval = 500,
  xtol_rel = 1e-05,
  lb_sigma = 10,
  ub_sigma = 1000,
  lb_theta = 0.5,
  ub_theta = 1000,
  lb_delta = -1,
  ub_delta = 1,
  seed = NULL,
  n_starts = 1,
  workers = 1
)
}
\arguments{
\item{X}{A numeric matrix or data frame. The first \code{q} input columns contain
quantitative levels and the next \code{q} columns contain sequence positions.
Each sequence row must be a permutation of \code{1:q}. A column named \code{y} may
be included as the response.}

\item{y}{An optional numeric response vector. It may be omitted when \code{X}
contains a response column named \code{y}.}

\item{q}{The number of components. It is inferred from the number of input
columns when omitted. The two-dimensional model requires at least three
components; the full model requires at least two.}

\item{tau}{A fixed nonnegative nugget variance added to the covariance
diagonal.}

\item{maxeval}{Maximum number of objective evaluations used by \code{nloptr}.}

\item{xtol_rel}{Relative parameter tolerance used by \code{nloptr}.}

\item{lb_sigma, ub_sigma}{Lower and upper bounds for the additive variance
parameters.}

\item{lb_theta, ub_theta}{Lower and upper bounds for the quantitative
correlation parameters.}

\item{lb_delta, ub_delta}{Lower and upper bounds for the mapping parameters.}

\item{seed}{An optional nonnegative integer used to generate the initial
parameter vectors. The first start retains the result produced by this
seed when \code{n_starts = 1}.}

\item{n_starts}{Number of independent parameter starts. The fitted object
contains the result with the lowest objective among the converged starts.}

\item{workers}{Number of local worker processes. Values greater than one use
a socket cluster and are capped at two or \code{n_starts}, whichever is lower.}
}
\value{
An object of class \code{magpfull}.
}
\description{
Fits the quantitative-sequence model with \code{q - 1} latent mapping dimensions,
giving the sequence positions a less constrained coordinate representation.
}
\details{
The data layout, quantitative scaling, and covariance construction
are the same as in \code{\link[=magp2d_fit]{magp2d_fit()}}. The difference is the number of latent
coordinates used to represent the sequence positions. With more than one
start, the function keeps the converged result with the lowest objective
and records the outcome of every start in \code{fit$multistart$starts}.
}
\examples{
\donttest{
train <- read.table(
  system.file("extdata", "example_train.txt", package = "magp"),
  header = TRUE
)
fit <- magpfull_fit(train, seed = 1, n_starts = 2)
fit
}
}
