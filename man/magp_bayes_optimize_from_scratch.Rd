% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrated-workflow.R
\name{magp_bayes_optimize_from_scratch}
\alias{magp_bayes_optimize_from_scratch}
\title{Start Bayesian optimization before any experiments have been run}
\usage{
magp_bayes_optimize_from_scratch(
  FUN,
  n_initial,
  q,
  model = c("2d", "full"),
  direction = c("minimize", "maximize"),
  n_iter = 10L,
  xi = 0,
  stop_ei = 0,
  stop_patience = 3L,
  seed = NULL,
  design_control = list(),
  fit_control = list(),
  acquisition_control = list(),
  objective_args = list(),
  verbose = TRUE
)
}
\arguments{
\item{FUN}{Function that evaluates one experiment. It must accept the
generated arguments \code{quantity_1}, ..., \code{quantity_q}, \code{sequence_1}, ...,
\code{sequence_q}. It must return one finite number, or a list with one finite
number named \code{Score} or \code{Value}.}

\item{n_initial}{Number of experiments in the generated initial design.}

\item{q}{Number of components. It must be at least three.}

\item{model}{MaGP mapping to fit. Use \code{"2d"} for the compact mapping or
\code{"full"} for the full mapping.}

\item{direction}{Use \code{"minimize"} when smaller responses are better and
\code{"maximize"} when larger responses are better.}

\item{n_iter}{Maximum number of new experiments selected after the initial
design has been evaluated.}

\item{xi}{Nonnegative expected-improvement offset. The default, \code{0}, uses
the current best response as the improvement target.}

\item{stop_ei}{Nonnegative early-stopping threshold for expected
improvement.}

\item{stop_patience}{Number of consecutive selected points with expected
improvement less than or equal to \code{stop_ei} required to stop early.}

\item{seed}{Optional nonnegative whole-number seed. It makes the initial
design, model starts, acquisition searches, and random values produced by
\code{FUN} reproducible while preserving the caller's random-number state.}

\item{design_control}{Optional named list passed to
\code{\link[=magp_initial_design]{magp_initial_design()}}. Use \code{sequence_method} to choose \code{"random"},
\code{"sfta"}, or \code{"sann"}. Other common choices include \code{sequence_maxit},
\code{sfta_control}, \code{quantity_maxit}, and \code{alignment_maxit}. This function
supplies \code{n}, \code{q}, and \code{seed}.}

\item{fit_control}{Optional named list passed to \code{\link[=magp2d_fit]{magp2d_fit()}} or
\code{\link[=magpfull_fit]{magpfull_fit()}}. Common choices are \code{maxeval}, \code{n_starts}, and \code{workers}.
This function supplies \code{X}, \code{y}, and \code{seed}.}

\item{acquisition_control}{Optional named list passed to
\code{\link[=magp_next_point]{magp_next_point()}}. Common choices are \code{lower}, \code{upper}, \code{sequences},
\code{n_starts}, \code{workers}, and \code{maxit}. This function supplies the fitted
model, direction, \code{xi}, current best response, and seed.}

\item{objective_args}{Optional named list of fixed arguments passed to \code{FUN}
in addition to the generated input columns.}

\item{verbose}{If \code{TRUE}, print one line after each initial and sequential
evaluation.}
}
\value{
An object of class \code{magp_bayes_opt} containing the initial design,
all completed evaluations, and the final fitted model.
}
\description{
Use this function when no initial data are available. It creates an initial
quantitative-sequence design, evaluates \code{FUN} at those runs, and then uses
MaGP expected improvement to select later runs.
}
\section{How to use this function}{

Define \code{FUN}, choose \code{n_initial} and \code{q}, and state whether the response
should be minimized or maximized. The function then:
\enumerate{
\item creates an initial design;
\item calls \code{FUN} once for each initial row;
\item fits the selected MaGP model; and
\item selects and evaluates as many as \code{n_iter} additional rows.
}

The generated columns are named \code{quantity_1} through \code{quantity_q} and
\code{sequence_1} through \code{sequence_q}. Use \code{\link[=magp_bayes_optimize]{magp_bayes_optimize()}} instead when
initial experiments and responses already exist.
}

\section{Reading the result}{

The result has the same main components as \code{\link[=magp_bayes_optimize]{magp_bayes_optimize()}}, including
\code{best_point}, \code{best_value}, \code{history}, the final \code{model}, and all recorded
search settings. It also contains:
\itemize{
\item \code{initial_design}: the generated quantitative-sequence design;
\item \code{initial_response}: the response from each initial run;
\item \code{initial_evaluations}: the number of initial runs; and
\item \code{design_control}: the initial-design settings that were used; and
\item \code{started_from_initial_design}: \code{TRUE}, marking that the workflow generated
its own starting design.
}
}

\examples{
\donttest{
objective <- function(quantity_1, quantity_2, quantity_3,
                      sequence_1, sequence_2, sequence_3) {
  quantities <- c(quantity_1, quantity_2, quantity_3)
  sequence <- c(sequence_1, sequence_2, sequence_3)
  -sum((quantities - c(0.2, 0.6, 0.8))^2) -
    0.01 * sum((sequence - c(1, 3, 2))^2)
}
result <- magp_bayes_optimize_from_scratch(
  FUN = objective,
  n_initial = 6,
  q = 3,
  direction = "maximize",
  n_iter = 1,
  seed = 1,
  design_control = list(
    sequence_maxit = 100,
    quantity_maxit = 100,
    alignment_maxit = 100
  ),
  fit_control = list(maxeval = 100),
  acquisition_control = list(n_starts = 2, maxit = 20),
  verbose = FALSE
)
result$initial_design$design
result$best_point
result$best_value
result$history
}

}
